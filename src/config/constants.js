export const DEFAULT_TIMEOUT = 30000;
export const DEFAULT_SIZE_LIMIT = 5 * 1024 * 1024; // 5MB
export const DEFAULT_SIMILARITY_THRESHOLD = 0.95;

export const AUTH_CONFIG = {
  tokenHeader: 'X-Inspector-Token',
  tokenExpiry: '24h'
};